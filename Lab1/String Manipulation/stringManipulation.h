#ifndef STRINGMANIPULATION_H
#define STRINGMANIPULATION_H

void reverse(char *myString);   // Reverse all characters.
void lowercase(char *myString); // Convert all characters to lowercase.
void uppercase(char *myString); // Convert all characters to UPPERCASE.
void titlecase(char *myString); // Convert all characters to Title Case.

#endif